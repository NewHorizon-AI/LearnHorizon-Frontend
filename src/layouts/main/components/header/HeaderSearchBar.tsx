import React from 'react'
import SearchBar from '@/components/ui/search'

const HeaderSearchBar: React.FC = () => {
  return <SearchBar className="ml-4 sm:ml-0 flex-1 sm:max-w-[20%] mr-4" />
}

export default HeaderSearchBar
