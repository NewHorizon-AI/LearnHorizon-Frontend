'use client'

import React, { useState, useEffect } from 'react'
import { type IPublication } from '@/interface/IBackend'

export default function Model() {
  const [model, setModel] = useState()

  return <div>BIENVENIDOS AL HIMALAYA</div>
}
